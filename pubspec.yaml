name: completer_ex
version: 4.0.2
homepage: https://github.com/bsutton/completer_ex
description: A wrapper for Completer which logs when the completer doesn't complete in a timely manner.
environment:
  sdk: '>=2.19.0 <4.0.0'
dependencies:
  simple_logger: ^1.8.0
  stacktrace_impl: ^2.0.0
dev_dependencies:
  lint_hard: ^4.0.0
  test: ^1.24.2
